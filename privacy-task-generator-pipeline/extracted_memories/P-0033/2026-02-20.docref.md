# 2026-02-20

## Timesheet closeout

Mark left this as a working-memory pointer for timesheet closeout. The operating
context is Core Infrastructure & Scalability in Cork, IE: Action: Mark to draft proposal for containerisation strategy by end of week for steering committee review. The memory should help a
trajectory recover the reason to open the artifact, without storing the private
values from it.

- Selected file: `document/timesheet.xlsx`.
- Read identifiers, dates, amounts, locations, credentials, health details, and
  names directly from the selected file when a task requires them.
- Do not copy sensitive values into memory or into a free-text summary.
- If rendered variants exist, use this exact path as the anchor unless the task
  explicitly asks for a visual version.

Next: cite the selected file path, derive values live, and keep this note as
context rather than evidence.
